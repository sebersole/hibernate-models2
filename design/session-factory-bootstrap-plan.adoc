= SessionFactory Bootstrap Plan
:toc:

This note sketches the next PoC direction: use the boot-model pipeline to build
enough ORM boot state to instantiate a `SessionFactoryImpl` / `EntityManagerFactory`.

The plan works backward from `org.hibernate.internal.SessionFactoryImpl` because
that constructor is the current runtime boundary.

== Constructor Boundary

`SessionFactoryImpl` currently accepts three inputs:

[source,java]
----
SessionFactoryImpl(
        MetadataImplementor bootMetamodel,
        SessionFactoryOptions options,
        BootstrapContext bootstrapContext)
----

Those inputs are not independent.  They are all products of bootstrap settings,
service-registry construction, source discovery, model categorization, and boot
model binding.

The PoC should make those products explicit rather than recreating the current
inline flow from `EntityManagerFactoryBuilderImpl` and `MetadataBuildingProcess`.

== Some Assumptions

First, a few assumptions we will be working under...

* We no longer need the split in building the EMF from JPA container.  This was something we did for Wild Fly to address a gap in the EE and JPA spec which should no longer be an issue.
* We will need to come back and address other Hibernate projects concerns.  Some of them contributor specialized builders, and we need to make sure those needs are addressed.
* Limit set of bootstrap entries -
** JPA "SE boostrap" - `Persistence`
** JPA "SE boostrap" - `(Hibernate)PersistenceConfiguration`
** JPA "EE bootstrap" - Ultimately `PeristenceUnitDescriptor`
** Legacy `Configuration`, but deprecated in favor of `HibernatePersistenceConfiguration`
** Maybe some form that accepts `AvailableResources` + settings?


== Current Constructor Responsibilities

The constructor still orchestrates a large amount of factory assembly:

* builds the `SessionFactoryServiceRegistry`
* creates the `EventEngine`
* initializes the boot metamodel with the factory
* resolves factory name, JNDI name, UUID, and masked settings
* creates `SqlStringGenerationContext`
* registers integrators and applies integration callbacks
* orders columns and validates the boot metamodel
* primes second-level cache regions
* creates `RuntimeMetamodelsImpl`
* creates `QueryEngineImpl` before runtime metamodel initialization
* creates `SqlTranslationEngine`
* creates and initializes `MappingMetamodelImpl`
* applies fetch profiles after mapping metamodel creation
* creates default session and stateless-session builders
* resolves current-session context
* extracts frequently used services
* scopes `TypeConfiguration` to the completed factory

That sequence is delicate.  The PoC should avoid changing all of it at once, but
it should use named bootstrap products so the handoff into this constructor is
clear.

== Proposed Flow

[source]
----
Bootstrap inputs
    -> ResolvedBootstrapSettings
    -> AvailableResources
    -> CategorizedDomainModel
    -> MetadataImplementor
    -> ResolvedSessionFactorySettings
    -> SessionFactoryBuildContext
    -> SessionFactoryImpl
----

The immediate goal is not to replace `SessionFactoryImpl`.  The immediate goal
is to produce its three required inputs through the new pipeline.

== Phase Products

=== ResolvedBootstrapSettings

Already prototyped.

Owns early boot settings that affect source collection and mapping processing.
It should not be a dumping ground for all session-factory behavior.

=== AvailableResources

Already prototyped.

Owns normalized source inventory:

* managed class details
* package-info class details
* already-bound XML mapping documents

It should not own XML interpretation, categorization, binding, or settings
resolution.

=== CategorizedDomainModel

Already prototyped.

Owns interpreted domain metadata before binding to ORM's boot mapping model.
This is currently where XML source interpretation lives because there is no
standalone prepared-source artifact.

=== MetadataImplementor

This is the first major bridge into existing ORM runtime bootstrap.

The PoC binder should produce a normal ORM boot mapping model that can satisfy
`SessionFactoryImpl` expectations:

* persistent classes
* collections
* database / namespace / table state
* type registrations and converter registrations
* filters, fetch profiles, named queries, imports, and global registrations
* cache region configuration

This is the main proving ground for the binding work.

=== ResolvedSessionFactorySettings

This is now prototyped as
link:../src/main/java/org/hibernate/boot/settings/ResolvedSessionFactorySettings.java[`ResolvedSessionFactorySettings`]
and
link:../src/main/java/org/hibernate/boot/settings/SessionFactorySettingsResolver.java[`SessionFactorySettingsResolver`].

`SessionFactoryOptions` is very large and mixes resolved values, service
references, JPA/native defaults, and raw setting interpretation.  A resolver
similar to `BootstrapSettingsResolver` makes the factory-build handoff more
explicit without immediately copying ORM's full options surface.

Potential responsibilities:

* apply JPA-vs-native defaults
* resolve factory name, UUID, observer list, interceptor, statement observer,
  statement inspector, and session defaults
* resolve tenant, cache, JDBC, transaction, statistics, query, and compliance
  options
* preserve a raw settings map for components that still require one
* expose the `StandardServiceRegistry` used for factory construction

The current prototype carries configuration values, JPA bootstrap mode, and the
first constructor-facing values needed by `SessionFactoryImpl`: standard service
registry, UUID, factory name/JNDI-name flag, statement observer, interceptor,
factory observers, Bean Validation `ValidatorFactory` reference, multi-tenancy
flag, current-tenant resolver, cache settings, default tenant id Java type,
default catalog, and default schema.  It should continue to grow one setting at
a time as `SessionFactoryBuilder` needs them.

The PoC also has a temporary
link:../src/main/java/org/hibernate/boot/orchestration/internal/SessionFactoryOptionsAdapter.java[`SessionFactoryOptionsAdapter`].
It adapts `ResolvedSessionFactorySettings` to ORM's existing
`SessionFactoryOptions` interface, but only for audited methods.  Unsupported
methods throw immediately so new dependencies are documented before being added.
The design point is to move option resolution out of the final constructor path
without quietly copying the full legacy options builder.

=== SessionFactoryBuildContext

This is proposed, not yet prototyped.

`BootstrapContext` currently spans early source collection, model building, and
runtime metamodel creation.  That makes it easy for early bootstrap concepts to
leak into late factory construction.

A later build context could hold only the values needed after boot model binding:

* `BootstrapContext` or a narrowed replacement for runtime model creation
* `MetadataImplementor`
* resolved session-factory options
* service registry
* `TypeConfiguration`
* resolved settings map
* `SqlStringGenerationContext`

This does not need to replace ORM's `BootstrapContext` immediately.  It can
start as a local planning object used by the PoC factory bootstrap.

== Improvements Worth Prototyping

=== Resolve SessionFactoryOptions Explicitly

Build a resolver that produces an adapter implementing ORM's
`SessionFactoryOptions` from resolved settings and services.

This makes the PoC path testable without forcing all option interpretation to
remain hidden in existing builders.

=== Split Early BootstrapContext From Late Build Context

Keep `BootstrapContext` for early model-building concerns.  Introduce a late
`SessionFactoryBuildContext` only when preparing to instantiate the factory.

The aim is to make the handoff visible:

[source]
----
MetadataImplementor + ResolvedSessionFactorySettings + BootstrapContext
    -> SessionFactoryBuildContext
    -> SessionFactoryImpl
----

=== Make Runtime Model Creation a Named Phase

`SessionFactoryImpl` currently performs runtime metamodel creation inline.  The
sequence is constrained by query-engine and SQL-translation dependencies.

For now, leave that logic in `SessionFactoryImpl`, but document it as a future
phase:

[source]
----
validated MetadataImplementor
    -> RuntimeMetamodels
    -> QueryEngine / SqlTranslationEngine
    -> fetch profile application
----

=== Treat Integrators as an Explicit Phase

Integrators currently run inside `SessionFactoryImpl` before boot metamodel
ordering, validation, cache priming, and runtime metamodel creation.

That phase should eventually be visible:

[source]
----
bound boot model
    -> integrators applied
    -> boot model ordered and validated
    -> runtime model created
----

=== Avoid Requiring an Incomplete SessionFactory

`SessionFactoryImpl` builds the session-factory service registry by passing
`this` before the factory is fully initialized.

That may remain unavoidable in the short term, but it is a useful design smell.
An eventual factory-build handle could allow services that need factory identity
to avoid depending on a partially initialized `SessionFactoryImpl`.

== Constructor Dependency Audit

Before adding more resolvers or compatibility adapters, audit each legacy
constructor dependency as an individual need.  The goal is to avoid preserving a
large contract simply because the current constructor happens to accept it.

[cols="1,2,2,1,2,2", options="header"]
|===
| Legacy contract
| Current consumers to inspect
| Actual values needed
| Phase
| Replacement candidate
| Status / notes

| `BootstrapContext`
| `SessionFactoryImpl`, runtime metamodel creation, type/configuration access,
  integrator callbacks
| Unknown.  Likely class-loading, bean access, `TypeConfiguration`, function
  registry, SQL functions, auxiliary objects, converters, cache region
  definitions, and JPA bootstrap flags.
| Early boot and late factory construction are currently mixed.
| Split into narrower early model-building context and late
  `SessionFactoryBuildContext`; keep an adapter only where ORM still requires
  `BootstrapContext`.
| Needs audit first.  Treat as suspicious because it is overly encompassing.

| `SessionFactoryOptions`
| `SessionFactoryImpl`, services, runtime model/query infrastructure, session
  builders
| Many setting-derived factory decisions: naming, observers, tenant/cache/JDBC,
  transaction, statistics, query, compliance, default session behavior, raw
  settings map.
| Factory construction and runtime behavior.
| `ResolvedSessionFactorySettings` plus adapter to ORM's interface if needed.
| Initial audit done.  This remains a runtime SPI view, so the likely first
  implementation is a narrow resolver plus adapter.  Audit ORM's
  `org.hibernate.boot.SessionFactoryBuilder` for the customization surface and
  `SessionFactoryOptionsBuilder` for the delegated resolution behavior.

| `MetadataImplementor`
| `SessionFactoryImpl`, runtime metamodel creation, cache priming, query/import
  registration, schema/database access
| Boot mapping model: entity/collection bindings, database model, type
  configuration, filters, fetch profiles, named queries, imports, cache regions.
| Handoff from boot-model binding to factory construction.
| Probably remains a bridge initially.  Longer term, consider whether a narrower
  immutable boot model can replace direct collector-style access.
| Main current proving ground for the PoC binder.

| `InFlightMetadataCollector`
| Binding internals, second passes, table/database registration, metadata
  validation/order, named objects, imports, type/converter registrations
| Unknown split between mutable binding state and final metadata output.
| Boot-model binding.
| Prefer `BindingState` as the mutable binding target if it can own the missing
  responsibilities.
| Audit `BindingState` gaps one by one before accepting collector dependence.

| `MetadataBuildingContext`
| Binders, naming/default resolution, table/column creation, access to bootstrap
  context and metadata collector
| Services, naming/options/defaults, type configuration, binding state, current
  contributor, temporal/audit defaults.
| Boot-model binding.
| Narrow binding context composed from services, resolved settings/defaults, and
  `BindingState`.
| Current PoC still uses ORM's root implementation; identify which methods the
  boot-model binder actually calls.

| `MetadataBuildingOptions`
| `MetadataBuildingContext`, metadata collector, naming strategies, column
  ordering, cache defaults, XML mapping flag, type options
| Mapping defaults and strategy choices used during binding and metadata
  validation.
| Boot-model binding.
| Resolved mapping options, possibly derived from `ResolvedBootstrapSettings`
  plus service-registry settings.
| Local copy may be useful if `MetadataBuilderImpl` remains too coupled.

| `SessionFactoryServiceRegistry`
| Constructed by `SessionFactoryImpl`; consumed by runtime services and
  integrators
| Service instances that may need factory identity, plus initiator-provided
  services.
| Factory construction.
| Factory-build handle or late service-registry creation phase.
| Audit whether services really require a fully constructed factory or just a
  build-time handle.
|===

Audit process for each row:

. Find concrete consumers in ORM.
. List the accessed methods/values rather than the nominal type.
. Assign each accessed value to source discovery, binding, factory option
  resolution, runtime model creation, or service construction.
. Decide whether the PoC should use the legacy contract, provide a narrow local
  replacement, or create a temporary adapter.
. Record required upstream follow-up in
  link:orm-integration-notes.adoc[`orm-integration-notes.adoc`] when a local
  copy or narrowed replacement is introduced.

=== BootstrapContext

Initial inspection target: `SessionFactoryImpl` and the first pass-through level
(`Integrator` and `RuntimeModelCreationContext` consumers).

Direct `SessionFactoryImpl` usage:

* `bootstrapContext.getTypeConfiguration()` is read at constructor start and
  stored as the factory `TypeConfiguration`.
* the full `BootstrapContext` is passed to `Integrator#integrate`.
* the full `BootstrapContext` is passed through
  `RuntimeModelCreationContext#getBootstrapContext()` during mapping/JPA
  runtime model creation.

This means the constructor itself does not appear to need the whole contract.
The broad need comes from extension and runtime-model pass-through.

[cols="1,2,2,2", options="header"]
|===
| Consumer area
| Observed access
| Likely narrower dependency
| Notes

| `SessionFactoryImpl`
| `getTypeConfiguration()`
| `TypeConfiguration`
| Direct constructor need.  This can be made explicit in a later
  `SessionFactoryBuildContext`.

| Integrators
| `Integrator#integrate(Metadata, BootstrapContext, SessionFactoryImplementor)`
| Compatibility adapter for existing `Integrator` contract
| This is an extension compatibility concern.  Existing integrators may use any
  `BootstrapContext` method, so narrowing probably requires a staged SPI change
  or adapter.

| Runtime model creation
| `RuntimeModelCreationContext#getBootstrapContext()`
| Runtime model creation context with explicit values
| Known downstream uses include class-loading access, bean registry/access,
  representation strategy selection, metadata building options, service registry,
  and type/configuration access.

| Runtime JPA metamodel
| `runtimeModelCreationContext.getBootstrapContext().getClassLoaderService()`
| `ClassLoaderService` or class-loading access
| Example direct use in `JpaMetamodelImpl`.

| Runtime representation strategies
| `getClassLoaderAccess()`, `getManagedBeanRegistry()`,
  `getRepresentationStrategySelector()`
| representation strategy creation services
| Examples include POJO/entity/embeddable representation strategy construction.

| Persister/runtime mapping construction
| `getMetadataBuildingOptions()`, `getServiceRegistry()`
| mapping/runtime creation options and service access
| Examples include collection/entity persister initialization and multi-table
  strategy support.
|===

Preliminary conclusion:

`BootstrapContext` should not be treated as a single required constructor
dependency.  For the PoC, keeping it as an adapter may be acceptable for
integrator compatibility and current runtime model creation, but the design
target should be to split it into:

* explicit direct constructor inputs such as `TypeConfiguration`;
* a narrower runtime model creation context;
* an integrator compatibility adapter until the extension SPI can be revisited;
* early boot-only services that should not reach `SessionFactoryImpl`.

==== Usage Buckets

The specific `BootstrapContext` uses still need to be classified.  The intended
classification is:

Early boot only::
Should not reach `SessionFactoryImpl`.
+
Likely examples include source discovery, XML processing, metadata binding
helpers, `ModelsContext`, temporary classloader access, and scanning artifacts.

Runtime model creation::
Should move onto, or remain available through, a narrower
`RuntimeModelCreationContext`.
+
Likely examples include `ClassLoaderService`, `ManagedBeanRegistry`,
representation strategy selection, and any mapping/runtime options that are
still proven necessary.

Integrator compatibility::
May require a legacy `BootstrapContext` adapter in the short term.
+
`Integrator#integrate(Metadata, BootstrapContext, SessionFactoryImplementor)` is
the strongest compatibility reason to preserve a `BootstrapContext` view until a
new extension SPI exists.

This classification has not been completed yet.  Each observed use should be
assigned to one of these buckets before introducing a replacement contract.

==== PoC Decision

The PoC should not treat `BootstrapContext` as a primary bootstrap product.
Where existing ORM integration points still require the legacy contract, the PoC
may create or carry an adapter.  New PoC phases should request explicit values
instead of accepting `BootstrapContext`.

==== Next Slice Guardrails

The next SessionFactory bootstrap slice may introduce a temporary
`BootstrapContext` adapter in order to call ORM's current `SessionFactoryImpl`.
That adapter is a compatibility device, not the target architecture.

Rules for that slice:

* support a `BootstrapContext` method only after identifying the concrete
  consumer that asked for it;
* keep unsupported methods failing loudly so new dependencies become audit
  items instead of accidental design;
* prefer explicit values or a narrower `SessionFactoryBuildContext` for new PoC
  code;
* preserve a compatibility view for existing ORM extension points such as
  `Integrator#integrate(Metadata, BootstrapContext, SessionFactoryImplementor)`;
* document every supported adapter method in this audit section.

The desired upstream shape is still expected to be narrower than
`BootstrapContext`: likely explicit factory-build inputs plus a runtime model
creation context, with a legacy adapter only where extension compatibility
requires it.

==== Existing Context Reuse

The first attempt did not need a new `BootstrapContext` adapter.  The
`MetadataImplementor` produced by the PoC is currently still an
`InFlightMetadataCollector`, and that collector exposes the ORM
`BootstrapContext` that was used during metadata binding.  Reusing that existing
context is a better compatibility step than inventing a second context view.

The next boundary was therefore not `BootstrapContext` construction.  It was the
handoff from in-flight metadata to finalized metadata:

* `SessionFactoryBuilder` can create the minimal `SessionFactoryOptions` adapter.
* `SessionFactoryBuilder` can locate the existing `BootstrapContext` from the
  in-flight metadata collector.
* `SessionFactoryImpl` should not be called with the in-flight collector because
  `InFlightMetadataCollectorImpl#initSessionFactory` deliberately rejects that
  path.
* `MetadataResolver#finalizeMetadata` now builds the finalized `MetadataImpl`
  using `InFlightMetadataCollectorImpl#buildMetadataInstance`.
* Finalized metadata runs ORM second-pass/exportable processing and can change
  finalized property/column ordering compared to the earlier in-flight collector
  view.

With finalized metadata in place, the current SessionFactory construction
boundary moved into runtime service creation.  The first newly observed
unsupported options method was `SessionFactoryOptions#isQueryCacheEnabled`,
asked for while creating cache services.  The cache-related options bucket has
now been added to `ResolvedSessionFactorySettings` and
`SessionFactoryOptionsAdapter`.

The next boundary after cache was `SessionFactoryOptions#getInterceptor`, which
belongs to the session defaults / integration hooks bucket rather than cache.
That method is now resolved from `SessionEventSettings.INTERCEPTOR`.

With the interceptor hack in place, `SessionFactoryImpl` reached metadata
validation and exposed a duplicate identifier-column binding gap.  The PoC fixed
that for basic identifiers by keeping the basic id as the identifier property
only, rather than also adding it to the normal declared-property list.

The query-engine and runtime-default options needed by the simple
SessionFactory smoke test are now resolved.  The old
`SessionFactoryOptions#getCustomSqlFunctionMap` boundary has been crossed.

The binding test helper now finalizes the in-flight collector with
`InFlightMetadataCollectorImpl#buildMetadataInstance`, then runs
`MetadataImplementor#orderColumns(false)` and `MetadataImplementor#validate()`
before invoking test assertions.  This makes the binder suite exercise the same
mapping validation pressure that exposed the duplicate identifier-column bug.
Tests for known unsupported validated shapes, such as composite-key `@Any` and
association-id join-table mappings, assert the validation failure explicitly.

Follow-up questions:

* Which `BootstrapContext` methods are actually used by built-in integrators,
  especially Bean Validation?
* Can `RuntimeModelCreationContext` expose the needed values directly and
  deprecate `getBootstrapContext()`?
* Does runtime model creation still need `MetadataBuildingOptions`, or only a
  smaller set of resolved mapping/runtime options?
* Can `TypeConfiguration` be produced/scoped without retaining an early
  `BootstrapContext` reference?


==== RuntimeModelCreationContext Usages

* ModelsContext
* ServiceRegistry
* _ClassLoaderService_ (available from ServiceRegistry)
* ManagedBeanRegistry
* _ManagedTypeRepresentationResolver_ (uses static impl)
* _CollectionSemanticsResolver_ (uses a static impl)


==== BeanValidationIntegrator Usages

* None

=== SessionFactoryOptions

Initial inspection target: `SessionFactoryImpl`,
`org.hibernate.boot.SessionFactoryBuilder`, `SessionFactoryOptionsBuilder`,
`SessionFactorySettings`, and the runtime session/JDBC contexts that read
options through `SessionFactoryImplementor`.

`SessionFactoryOptions` is different from the earlier constructor dependencies.
It is accepted by the `SessionFactoryImpl` constructor, but it is also stored and
returned by `SessionFactoryImplementor#getSessionFactoryOptions()`.  That means
the PoC cannot treat it purely as a temporary construction object unless the
runtime SPI is also changed upstream.

Direct `SessionFactoryImpl` constructor usage:

* stores the full options reference as the factory's runtime options view;
* resolves `StatementObserver`, `uuid`, `SessionFactoryObserver[]`, factory
  name/JNDI name, tenant id Java type, default catalog/schema, and
  multi-tenancy enablement;
* obtains the `StandardServiceRegistry` from the options to build the
  `SessionFactoryServiceRegistry`;
* passes the options to `QueryEngineImpl`;
* passes the options through `RuntimeModelCreationContext` during runtime model
  creation.

The constructor itself only dereferences a modest subset, but retaining the
object makes the full SPI available after construction.  Runtime consumers still
read many individual settings through the factory, including session defaults,
JDBC behavior, query/cache behavior, JPA compliance, dirty checking, tenant
resolution, JSON/XML format mappers, and graph parser behavior.

[cols="1,2,2,2", options="header"]
|===
| Consumer area
| Observed access
| Likely narrower dependency
| Notes

| `SessionFactoryImpl` identity and registration
| `getUuid()`, `getSessionFactoryName()`,
  `isSessionFactoryNameAlsoJndiName()`
| factory identity settings
| Used for registry, serialization, and optional JNDI naming.  These are good
  candidates for named fields on `ResolvedSessionFactorySettings`.

| Service-registry creation
| `getServiceRegistry()`, then
  `SessionFactoryServiceRegistryFactory#buildServiceRegistry(factory, options)`
| late factory service-registry creation input
| This is the strongest reason a compatibility adapter may be needed first.
  The service-registry builder receives both the incompletely constructed
  factory and the options object.

| Constructor-local behavior
| `getStatementObserver()`, `getSessionFactoryObservers()`,
  `isMultiTenancyEnabled()`, `getDefaultTenantIdentifierJavaType()`,
  `getDefaultCatalog()`, `getDefaultSchema()`
| explicit construction settings
| These could be direct fields on `ResolvedSessionFactorySettings` or part of a
  narrower `SessionFactoryBuildContext`.

| Runtime model/query creation
| `QueryEngineImpl(..., options, ...)` and
  `RuntimeModelCreationContext#getSessionFactoryOptions()`
| query/runtime model settings
| `SessionFactoryOptions` extends `QueryEngineOptions`, so query engine
  settings may deserve their own later slice rather than being mixed into the
  whole factory settings contract immediately.

| Session defaults
| `getInterceptor()`, session-scoped interceptor supplier,
  `getStatementInspector()`, default event listeners, default cache/flush/lock
  modes, default session properties, default batch/subselect/fetch settings,
  auto-close/flush-before-completion/JTA flags
| session-creation defaults
| These are runtime defaults used by `SessionBuilderImpl`,
  `StatelessSessionBuilderImpl`, `AbstractSharedSessionContract`, and
  `JdbcSessionContextImpl`.

| JDBC/runtime SQL behavior
| scrollable results, generated keys, JDBC fetch size, batch size, connection
  handling, provider autocommit behavior, SQL comments, JDBC time zone,
  default null precedence, parameter padding
| JDBC/runtime SQL settings
| Some values depend on `JdbcServices`, dialect defaults, and extracted JDBC
  metadata, so resolution is not just a raw map projection.

| Cache behavior
| second-level cache, query cache, query cache layout, timestamps cache factory,
  region prefix, minimal puts, structured/direct entries, collection cache
  auto-eviction
| cache settings plus cache service capabilities
| Resolution currently branches on the configured `RegionFactory`, so this
  belongs in the SessionFactory settings slice rather than pure bootstrap
  settings.

| Multi-tenancy
| enabled flag, tenant identifier resolver, tenant schema mapper, tenant
  credentials mapper, default tenant id Java type
| tenant runtime settings
| Enablement is service-driven in ORM today.  The resolver probably needs both
  resolved configuration values and the service registry.

| Integration hooks
| CDI BeanManager reference, ValidatorFactory reference, custom entity dirtiness
  strategy, entity name resolvers, entity-not-found delegate, observers
| integration/runtime extension settings
| These are user-provided objects or strategy-resolved services.  They are a
  good reason to keep the raw configuration map available even while promoting
  commonly used values to named fields.

| Mapping/type format settings used at runtime
| preferred SQL type codes, Java time/native enum/locale handling,
  timezone storage, JSON/XML format mappers, XML legacy format flag
| resolved type/format settings shared with mapping/runtime
| Some of these overlap conceptually with `ResolvedMappingSettings`.  Keep them
  in the raw settings root until a concrete consumer forces a split.
|===

Preliminary conclusion:

The closest conceptual corollary to `SessionFactorySettingsResolver` is ORM's
`org.hibernate.boot.SessionFactoryBuilder`: it represents the factory-build
customization surface.  That distinction is not very helpful mechanically,
however, because ORM's builder currently delegates most of that state into
`SessionFactoryOptionsBuilder`.  For the audit, treat the two together:
`SessionFactoryBuilder` identifies externally supported customization points,
while `SessionFactoryOptionsBuilder` shows how those points are currently
resolved and accumulated.

`SessionFactoryOptionsBuilder` should still not become the design target.  It
mixes several concerns:

* normalized raw configuration values;
* service/dialect/JDBC-metadata-derived decisions;
* query engine options;
* cache options;
* session defaults;
* integration object references;
* the legacy runtime `SessionFactoryOptions` SPI view.

The practical first step is still likely an adapter implementing ORM's
`SessionFactoryOptions`, backed by `ResolvedSessionFactorySettings`.  The
important design point is that `ResolvedSessionFactorySettings` should grow by
consumer bucket, not by blindly mirroring every method in the legacy interface.

That first adapter slice now exists as
link:../src/main/java/org/hibernate/boot/orchestration/internal/SessionFactoryOptionsAdapter.java[`SessionFactoryOptionsAdapter`].
It supports only the methods used by the direct `SessionFactoryImpl`
constructor path identified above.  The PoC `SessionFactoryBuilder` creates the
adapter and can find the existing `BootstrapContext` from the in-flight metadata
collector.  It still stops before constructing the factory because the metadata
product must be finalized first.

==== PoC Decision

For the next factory-bootstrap slice:

* keep the PoC `SessionFactoryBuilder` returning `SessionFactoryImplementor`;
* have `SessionFactorySettingsResolver` produce `ResolvedSessionFactorySettings`
  from `ResolvedBootstrapSettings` and a `ServiceRegistry`;
* keep the raw immutable configuration map on `ResolvedSessionFactorySettings`;
* add named fields only for values required by the immediate
  `SessionFactoryImpl` construction path;
* compare the growing settings surface against ORM's
  `org.hibernate.boot.SessionFactoryBuilder` customization methods, while also
  checking the delegated `SessionFactoryOptionsBuilder` behavior;
* introduce a temporary `SessionFactoryOptions` adapter if needed to instantiate
  ORM's current `SessionFactoryImpl`;
* avoid copying query/cache/session-default resolution until a real consumer
  requires it.

==== Minimal Adapter Slice

Implemented in the PoC:

* `SessionFactorySettingsResolver#resolve(ResolvedBootstrapSettings,
  ServiceRegistry)` requires a `StandardServiceRegistry` and resolves the
  constructor-facing settings listed above.
* `ResolvedSessionFactorySettings` keeps those values as named immutable fields
  alongside the raw configuration map.
* `SessionFactoryOptionsAdapter` exposes the audited subset through ORM's
  `SessionFactoryOptions` SPI.
* `SessionFactoryBuilder` now creates that adapter, verifies that an existing
  `BootstrapContext` is reachable from the current metadata product, and can
  build a `SessionFactoryImplementor` for the current simple-entity smoke test.

==== Cache Options Slice

Implemented in the PoC:

* `SessionFactorySettingsResolver` resolves cache settings using the configured
  `RegionFactory`, `CacheSettings`, and `StrategySelector`.
* `ResolvedSessionFactorySettings` now carries second-level cache enablement,
  query cache enablement, query cache layout, timestamps cache factory, cache
  region prefix, minimal puts, structured entries, direct-reference entries, and
  collection-cache auto-eviction.
* `SessionFactoryOptionsAdapter` exposes those values through the corresponding
  `SessionFactoryOptions` cache methods.

This moved the SessionFactory construction boundary past cache service creation
to the session-defaults/integration-hooks and query-engine options buckets,
which are now represented as named values in `ResolvedSessionFactorySettings`.
The subsequent duplicate identifier-column validation failure was fixed in the
basic-id binder.

==== Runtime Defaults and Query Options Slice

Implemented in the PoC:

* `ResolvedSessionFactorySettings` now carries the query-engine options needed
  by `QueryEngineImpl`, including custom function/translator hooks, JPA
  compliance, criteria value handling, immutable-entity update handling, JSON/XML
  function flags, portable integer division, and native JDBC parameter handling.
* `ResolvedSessionFactorySettings` also carries runtime default options needed
  while creating persisters, loaders, and default session builders, including
  statement inspector, session cache mode, connection handling, JDBC time zone,
  auto-close/flush-before-completion, identifier rollback, SQL comments, batch
  and subselect fetch defaults, maximum fetch depth, temporal table strategy,
  audit strategy, JPA callbacks, and collections-in-default-fetch-group.
* `MetadataResolver` now performs the dialect type-contribution step before
  binding so the bootstrap `TypeConfiguration` has DDL type descriptors needed by
  temporary table strategy creation.
* `SessionFactoryBuilder` re-scopes `TypeConfiguration` to the metadata building
  context before calling `SessionFactoryImpl`, preserving the boot-time access
  expected by dialect function registration.

The simple `SessionFactoryBuilderTests` smoke test now builds and closes a real
`SessionFactoryImplementor` and verifies that the runtime mapping metamodel
contains the simple entity descriptor.

The adapter is intentionally incomplete.  A failure from an unsupported
`SessionFactoryOptions` method is not just a missing implementation; it is a
prompt to classify that method into one of the buckets above before growing
`ResolvedSessionFactorySettings`.

Follow-up questions:

* Can `SessionFactoryServiceRegistryFactory#buildServiceRegistry` accept a
  narrower build context instead of the full options SPI?
* Should `QueryEngineOptions` become a separate resolved slice before building
  `QueryEngineImpl`?
* Which runtime users truly need access through
  `SessionFactoryImplementor#getSessionFactoryOptions()` and which could use
  narrower factory methods?
* Should Bean Validation's mutation of `setCheckNullability` be replaced by a
  build-time validation decision rather than a mutable options setter?

=== MetadataImplementor

Not audited yet.

=== InFlightMetadataCollector

Initial inspection target: PoC orchestration and binding code.

`InFlightMetadataCollector` is currently the mutable ORM metadata target.  The
PoC uses it both directly and indirectly through `MetadataBuildingContext`.
Unlike `MetadataBuildingContext`, most collector uses are not merely accidental
service access; many are real writes to the ORM boot metadata model that is
returned as `MetadataImplementor`.

Observed usage in the PoC:

[cols="1,2,2,2", options="header"]
|===
| Consumer area
| Observed access
| Likely narrower dependency
| Notes

| `MetadataResolver`
| creates `InFlightMetadataCollectorImpl`; after binding calls
  `orderColumns(false)` and `validate()` on the metadata result
| metadata target factory plus finalization step
| This is the clearest orchestration-level dependency.  Longer term this could
  become a `MetadataBuildTarget` / finalizer pair, but for now it is the bridge
  that proves the PoC can produce ORM metadata.

| `DomainModelCategorizer`
| reads `getPersistenceUnitMetadata()`, `getConverterRegistry()`, and
  `getDatabase()` from the collector before constructing `CategorizationContext`
| persistence-unit metadata, converter registry, database
| The categorization model no longer sees the collector.  Remaining reads are
  entry-point plumbing while `DomainModelCategorizer` still accepts
  `MetadataBuildingContext`.

| `BindingStateImpl`
| reads `getDatabase()` during construction; writes `addEntityBinding`,
  `addMappedSuperclass`, and `addFilterDefinition`
| database, managed-type registration target, filter-definition target
| Partly done: `BindingState` now localizes these writes.  Entity bindings still
  publish immediately because property-reference binding validates against the
  collector during the same binding run; mapped-superclass, collection, import,
  filter, and global-registration writes use the explicit final bridge.

| `BindingCoordinator` global registrations
| writes identifier generators, named entity graphs, converters, Java/JDBC type
  registrations, user/composite user types, collection type registrations, and
  embeddable instantiators
| global-registration target
| Done for non-query registrations: these now write to `BindingState` and are
  applied to the collector by the final bridge.  Query registrations still use
  ORM's legacy query binders.

| Query binding
| `QueryBinder.bindQuery`, `bindNativeQuery`, and
  `bindNamedStoredProcedureQuery` require `MetadataBuildingContext`
| query registration adapter
| This likely waits for upstream integration.  ORM's query binders currently
  consume the legacy context and write to the collector internally.

| Table binding
| `addTable` and `addDenormalizedTable`; table naming callbacks also expose
  `MetadataBuildingContext`
| database namespace/table factory
| This is a core collector responsibility today.  A narrow table factory would
  be cleaner, but ORM's `Table` creation and namespace registration currently
  live on the collector.

| Collection binding
| `addCollectionBinding`
| collection metadata target
| Done: collection binders now record collection bindings in `BindingState`;
  the collector remains the final storage target.

| Entity and mapped-superclass imports
| `addImport`
| import registration target
| Done: imports now record in `BindingState` and publish during the final bridge.

| Unique property references
| `addUniquePropertyReference`
| property-reference registration target
| Still an immediate collector side channel, but now localized behind
  `BindingState#addUniquePropertyReference`.  It cannot wait for the final
  bridge because later binding phases consume it during the same binding run.
  Upstream should ideally grow a typed property-reference API.

| Tenant-id binding
| reads `getTypeConfiguration()` and `getFilterDefinition()`; may write
  `addFilterDefinition`
| type configuration plus filter-definition registry
| Partly done: type configuration and filter lookup/write now go through
  `BindingState` so tenant-id validation sees pending filter definitions.

| `BindingOptionsImpl`
| reads `getDatabase().getJdbcEnvironment()`
| JDBC environment
| This is settings/defaults support, not a conceptual collector dependency.
|===

Preliminary conclusion:

`InFlightMetadataCollector` is not just an overly broad service locator.  In the
current bridge it is the mutable ORM metadata product.  The healthier split is:

* keep the collector as the short-term final metadata target;
* hide simple writes behind `BindingState` or a future boot metadata target
  (`addEntityBinding`, `addMappedSuperclass`, `addCollectionBinding`,
  `addImport`, filter definitions, global registrations);
* isolate table/namespace creation separately if upstream ORM is changed;
* leave query binding and mapping-object construction on the legacy adapter until
  those upstream APIs can be changed;
* keep final ordering/validation as an explicit orchestration step.

This audit suggests that removing collector exposure locally is useful only when
it improves binder shape without fighting ORM's mapping constructors.  The PoC
now uses `BindingState#applyMetadataRegistrations` as the explicit bridge for
simple metadata writes that can wait until the end of binding.  Entity bindings
and unique property references remain immediate but are localized behind
`BindingState` because later binding phases depend on the collector side effects.
Table creation and query binding still need upstream work before a local rewrite
is worthwhile.

=== MetadataBuildingContext

Initial inspection target: PoC categorization, binding, and orchestration code.

`MetadataBuildingContext` is not currently part of the `SessionFactoryImpl`
constructor boundary, but it is a major reason the PoC still creates an ORM
`BootstrapContext`, `MetadataBuildingOptions`, and `InFlightMetadataCollector`.
It is also passed into many ORM mapping model constructors.

Observed usage in the PoC:

[cols="1,2,2,2", options="header"]
|===
| Consumer area
| Observed access
| Likely narrower dependency
| Notes

| `MetadataResolver`
| Creates `AvailableResourcesContext` from
  `metadataBuildingContext.getBootstrapContext().getModelsContext()` and
  `getServiceRegistry()`; reads `getMetadataCollector()` after binding.
| `ModelsContext`, `ServiceRegistry`, final metadata result
| Source collection no longer needs the whole context.  This is already mostly a
  convenience leak from current orchestration.

| `DomainModelCategorizer`
| `getBootstrapContext()`, `getMetadataCollector().getPersistenceUnitMetadata()`,
  `getEffectiveDefaults()`, `getBuildingOptions().getMappingDefaults()`
| models context, XML persistence-unit metadata, effective mapping defaults,
  converter/global registration/database access, shared cache mode
| Categorization still receives `MetadataBuildingContext` at its entry point for
  now, but `CategorizationContext` no longer exposes it to the categorization
  model.

| `CategorizationContextImpl`
| direct constructor inputs: persistence-unit metadata, effective defaults,
  class details registry, shared cache mode, global registrations, converter
  registry, database
| explicit categorization collaborators
| Done: this no longer adapts or exposes `MetadataBuildingContext` or
  `BootstrapContext`.

| `BindingStateImpl`
| Stores the full context; initializes `Database` from
  `getMetadataCollector().getDatabase()` and `JdbcServices` from
  `getBootstrapContext().getServiceRegistry()`
| database, JDBC services, mutable binding target
| `BindingState` already owns much of the real binding state, but still exposes
  `getMetadataBuildingContext()` as an escape hatch.

| Binding coordinator/global registration binding
| Frequent `getMetadataCollector()` calls to add named queries, result-set
  mappings, fetch profiles, filters, imports, named entity graphs, and other
  global metadata.
| typed global-registration appliers on `BindingState` or a boot metadata target
| This is one of the main places where `BindingState` could absorb collector
  side effects.

| Mapping object construction
| `new RootClass(context)`, `new BasicValue(context)`, `new Component(context)`,
  `new ManyToOne(context, table)`, collection mappings, `Join`, `Table`
  creation through collector APIs
| legacy ORM mapping-construction context or local adapter
| This is the strongest short-term reason to keep an ORM-compatible
  `MetadataBuildingContext`.

| `BindingOptionsImpl`
| `getBuildingOptions().getMappingDefaults()`,
  `getBootstrapContext().getServiceRegistry().getService(ConfigurationService)`,
  `getMetadataCollector().getDatabase().getJdbcEnvironment()`
| resolved mapping defaults, configuration values, JDBC environment
| Can become independent of `MetadataBuildingContext`.

| `BindingContextImpl`
| currently constructed from `BootstrapContext` to extract metadata-building
  options and expose bootstrap services
| global registrations, naming strategies, shared cache mode, models/services
| This should likely depend on explicit values or a narrower binding services
  contract.

| Default cascade and naming/default lookups
| `getEffectiveDefaults().getDefaultCascadeTypes()`,
  `getObjectNameNormalizer()`, mapping defaults from `getBuildingOptions()`
| effective mapping defaults and name normalization
| Some of this may belong in resolved mapping options; object-name normalization
  may require a small adapter while ORM mapping objects remain in use.
|===

Methods not yet observed as central in the PoC audit:

* `getTypeDefinitionRegistry()`
* `getCurrentContributorName()`
* `getTemporalTableStrategy()`
* `getAuditStrategy()`

They may still be used indirectly by ORM mapping objects during validation or
runtime model creation, so this is not a final removal claim.

Preliminary conclusion:

`MetadataBuildingContext` is mainly a legacy aggregation point for three
different concerns:

* services/options/defaults needed during categorization and binding;
* the mutable metadata collector / binding target;
* ORM mapping object construction APIs that require the legacy context type.

The PoC should not treat it as the conceptual binding contract.  The likely
direction is:

* `CategorizationContext` is now independent of `MetadataBuildingContext`;
* continue growing `BindingState` into the typed mutable binding target;
* make `BindingOptions` / `BindingContext` consume explicit values;
* retain a small ORM-compatible `MetadataBuildingContext` adapter only where
  mapping object constructors or collector APIs still require it.

This audit has not been completed yet.  The next useful step is to list the
remaining `BindingState#getMetadataBuildingContext()` callers and decide whether
each one is:

* a real ORM mapping-constructor requirement;
* a collector side effect that should move to `BindingState`;
* a settings/defaults lookup that should move to `BindingOptions` or
  `BindingContext`;
* or a categorization concern that should move earlier.

Follow-up questions:

* Can `BindingState` hide `MetadataBuildingContext` from most binders?
* Which collector writes are missing typed methods on `BindingState`?
* Can ORM mapping constructors be isolated behind local factory methods so the
  legacy context dependency is contained?
* Can `CategorizationContext` be built directly from resolved settings,
  `AvailableResources`, and metadata target services?

=== MetadataBuildingOptions

Initial inspection target: PoC orchestration and binding code.

`MetadataBuildingOptions` is still created by ORM's `MetadataBuilderImpl` and
threaded through `MetadataBuildingContext`.  In the PoC it acts mostly as a
container for resolved mapping options and naming strategies, not as a mutable
metadata target.

Observed usage in the PoC:

[cols="1,2,2,2", options="header"]
|===
| Consumer area
| Observed access
| Likely narrower dependency
| Notes

| `MetadataResolver`
| obtains `metadataBuilder.getMetadataBuildingOptions()`; passes it to
  `InFlightMetadataCollectorImpl` and `MetadataBuildingContextRootImpl`; uses
  `getMappingDefaults()` to create `RootMappingDefaults`
| ORM bootstrap adapter inputs, mapping defaults
| This remains part of the temporary ORM bridge while we reuse
  `MetadataBuilderImpl`, `InFlightMetadataCollectorImpl`, and
  `MetadataBuildingContextRootImpl`.

| `BindingOptionsImpl`
| reads `getMappingDefaults().shouldImplicitlyQuoteIdentifiers()`,
  `getImplicitCatalogName()`, and `getImplicitSchemaName()`
| resolved mapping defaults plus quoting policy
| Good candidate for a narrow `ResolvedMappingSettings` or richer
  `BindingOptions` constructor.  It also reads skip-column-definition quoting
  from `ConfigurationService`, so the real input is settings plus mapping
  defaults plus `JdbcEnvironment`.

| `BindingContextImpl`
| reads `getImplicitNamingStrategy()`, `getPhysicalNamingStrategy()`, and
  `getSharedCacheMode()`
| naming strategies and shared cache mode
| These are stable binding services/options.  They can be passed explicitly once
  `BindingContextImpl` stops adapting `BootstrapContext`.

| `TableBinder`
| reads implicit and physical naming strategies through
  `bindingContext.getBootstrapContext().getMetadataBuildingOptions()`
| naming strategies
| This is redundant with `BindingContext`, which already exposes both
  strategies.  This can be cleaned locally without upstream API changes.

| `EntityTypeBinder` cache handling
| reads `getSharedCacheMode()`
| shared cache mode
| `BindingContext` already exposes shared cache mode, so this can be cleaned
  locally.

| `EntityTypeBinder` joined inheritance discriminator handling
| reads `ignoreExplicitDiscriminatorsForJoinedInheritance()` and
  `createImplicitDiscriminatorsForJoinedInheritance()`
| joined-inheritance discriminator settings
| Done locally: these are resolved onto `ResolvedMappingSettings`, copied into
  `BindingOptions`, and consumed by `EntityTypeBinder` from there.

| Soft-delete column naming
| reads `getPhysicalNamingStrategy()`
| physical naming strategy
| This can use `BindingContext#getPhysicalNamingStrategy()` locally.

| `DomainModelCategorizer`
| reads `getSharedCacheMode()` and `getMappingDefaults()` for
  `RootMappingDefaults`
| shared cache mode, mapping defaults
| Categorization already passes shared cache mode through
  `CategorizationContext`; the remaining mapping defaults are for XML processing
  and root mapping defaults construction.
|===

Preliminary conclusion:

`MetadataBuildingOptions` is less problematic than `MetadataBuildingContext` and
less central than `InFlightMetadataCollector`: it mostly contains values we do
need, but under a broad legacy container.

The likely direction is:

* keep creating it while the PoC reuses ORM's metadata builder/collector/context
  bridge;
* move local consumers toward explicit values already available from
  `BindingContext` and `BindingOptions`;
* grow a named mapping-settings contract for joined-inheritance discriminator
  behavior, implicit catalog/schema, global quoting, naming strategies, and
  shared cache mode;
* keep `MetadataBuildingOptions` as an adapter detail until ORM mapping
  constructors and XML/query processors no longer require the legacy context.

Near-term local cleanup candidates:

* have `TableBinder` use `BindingContext#getImplicitNamingStrategy()` and
  `getPhysicalNamingStrategy()`;
* have `EntityTypeBinder` use `BindingContext#getSharedCacheMode()` and
  `getPhysicalNamingStrategy()`;
* continue expanding `ResolvedMappingSettings` / `BindingOptions` only for
  settings that are actually consumed during model processing.

=== SessionFactoryServiceRegistry

Not audited yet.

== Initial Prototype Target

The first implementation target should be deliberately narrow:

[source]
----
AvailableResources
    -> DomainModelCategorizer
    -> BindingCoordinator
    -> MetadataImplementor
    -> existing SessionFactoryOptions adapter
    -> existing BootstrapContext
    -> new SessionFactoryImpl(...)
----

Success criteria:

* a small annotated model can bootstrap a usable `SessionFactory`
* the PoC binder produces a `MetadataImplementor` accepted by the constructor
* option resolution is explicit enough to see which settings are still missing
* no new abstraction is added unless it removes an actual handoff ambiguity

== Current PoC Slice

The current metadata target resolver is:

* link:../src/main/java/org/hibernate/boot/settings/BootstrapSettingsResolver.java[`BootstrapSettingsResolver`]
* link:../src/main/java/org/hibernate/boot/orchestration/MetadataResolver.java[`MetadataResolver`]
* link:../src/main/java/org/hibernate/boot/orchestration/ResolvedMetadata.java[`ResolvedMetadata`]

Settings resolution is a separate target:

[source]
----
configuration values / JPA inputs
    -> BootstrapSettingsResolver
    -> ResolvedBootstrapSettings
----

`MetadataResolver#resolve` owns only the metadata target phase order:

[source]
----
ResolvedBootstrapSettings + BootstrapSourceContributions + ServiceRegistry
    -> MetadataBuildingContext
    -> AvailableResources
    -> DomainModelCategorizer
    -> BindingCoordinator
    -> order columns
    -> validate
    -> ResolvedMetadata
----

`ResolvedMetadata` carries the finalized `MetadataImplementor`, the
`CategorizedDomainModel`, the `BindingState` used as the local metadata-write
bridge, and the `ResolvedBootstrapSettings`.

Later slices should add one resolver per gross target rather than growing a
single SessionFactory-wide orchestrator.

`SessionFactoryBuilder` is the next target shell.  It accepts
`ResolvedSessionFactorySettings`, `ResolvedMetadata`, and a `ServiceRegistry`,
and its intended return type is the runtime `SessionFactoryImplementor` itself.
There is no separate build-result object unless the factory construction path
proves it needs additional materialized products.

== Open Questions

* Should `ResolvedSessionFactorySettings` later produce an adapter implementing
  ORM's `SessionFactoryOptions`, or should the adapter itself be the resolved
  object?
* Which settings belong in `ResolvedBootstrapSettings` versus
  `ResolvedSessionFactorySettings`?
* Can `BootstrapContext` be narrowed before integration, or should the PoC simply
  reuse ORM's current implementation until the factory path works?
* Should XML source interpretation remain in categorization internals, or should
  a prepared-source phase emerge if more source interpretation steps appear?
* Can service-registry creation be moved before `SessionFactoryImpl` without
  breaking services that expect a concrete factory reference?
